<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.wangrollin.ai</groupId>
    <artifactId>ai-java-sdk-parent</artifactId>
    <version>0.3.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>ai-java-sdk-parent</name>
    <description>Parent build for the production-oriented Java SDK for backend AI integrations.</description>
    <url>https://github.com/wangrollin/ai-java-sdk</url>
    <inceptionYear>2026</inceptionYear>

    <modules>
        <module>core</module>
        <module>micrometer</module>
        <module>opentelemetry</module>
        <module>spring-boot-starter</module>
        <module>examples/support-ticket-triage</module>
        <module>examples/knowledge-base-rag</module>
    </modules>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>wangrollin</id>
            <name>wangrollin</name>
            <email>wangrollin@foxmail.com</email>
            <roles>
                <role>maintainer</role>
            </roles>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/wangrollin/ai-java-sdk.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/wangrollin/ai-java-sdk.git</developerConnection>
        <url>https://github.com/wangrollin/ai-java-sdk</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/wangrollin/ai-java-sdk/issues</url>
    </issueManagement>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
        <jackson.version>2.22.1</jackson.version>
        <jackson.annotations.version>2.21</jackson.annotations.version>
        <junit.version>6.1.1</junit.version>
        <micrometer.version>1.16.4</micrometer.version>
        <opentelemetry.version>1.55.0</opentelemetry.version>
        <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
        <maven-javadoc-plugin.version>3.10.1</maven-javadoc-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <spring-boot.version>4.0.5</spring-boot.version>
        <spring-framework.version>7.0.6</spring-framework.version>
        <spring-boot-3.version>3.5.16</spring-boot-3.version>
        <spring-framework-6.version>6.2.19</spring-framework-6.version>
        <assertj.version>3.27.7</assertj.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>io.wangrollin.ai</groupId>
                <artifactId>ai-java-sdk</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-core</artifactId>
                <version>${micrometer.version}</version>
            </dependency>

            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>${opentelemetry.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${build-helper-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>enforce-build-environment</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[17,)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                </requireMavenVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
          Keep Spring Boot 4 as the default development line while providing a stable,
          reproducible command for verifying the maintained Spring Boot 3.5 line. Both
          framework properties must move together because the example modules declare
          Spring Framework dependencies directly.
        -->
        <profile>
            <id>spring-boot-3</id>
            <properties>
                <spring-boot.version>${spring-boot-3.version}</spring-boot.version>
                <spring-framework.version>${spring-framework-6.version}</spring-framework.version>
            </properties>
        </profile>
    </profiles>
</project>
